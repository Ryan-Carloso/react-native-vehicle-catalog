{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "start": "json-server --watch db.json --port 3001 --static .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "json-server": "1.0.0-beta.6"
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.56.0"
  }
}
